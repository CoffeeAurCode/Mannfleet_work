/**
 * Canonical contact addresses.
 *
 * Every booking / reservation query is routed to BOOKING_EMAIL — import it
 * rather than hard-coding the address at each call site.
 */

export const BOOKING_EMAIL = "support@mannfleetpartners.com";
export const GENERAL_EMAIL = "info@mannfleetpartners.com";
export const WHATSAPP_NUMBER = "919810008008";
